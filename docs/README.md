# /docs — architecture, demo script, submission answers

- `architecture.md` — the diagram + component notes (start from `AGENTS.md §3`).
- `demo-script.md` — the < 3 min video script; lead with cross-session memory recall.
- `submission.md` — text description + Product Feedback for every tool/API/SDK/AWS service.
- `mcp-contract.md` — the frozen MCP tool contract (`AGENTS.md §5`).
